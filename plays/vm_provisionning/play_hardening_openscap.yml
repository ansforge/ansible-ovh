---
- name: Hardening AlmaLinux with OpenSCAP
  hosts: "{{ HOSTS }}"
  become: true
  vars:
    openscap_profiles:
      - xccdf_org.ssgproject.content_profile_cis_server_l1
  roles:
    - role_hardening_openscap
